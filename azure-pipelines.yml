# trigger:
# - master

pool:
  vmImage: "ubuntu-latest"

container: cr.microsoft.com/dotnet/core/sdk:2.2

steps:
  - script: |
      dotnet restore
      dotnet build
    displayName: "Build"

  - script: |
      dotnet test
    displayName: "Unit Test"
